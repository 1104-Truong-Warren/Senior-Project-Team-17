using UnityEngine;

public class GridStat : MonoBehaviour
{


    //sets the grid to start at position 0,0 (where 0,0 is assumed as the bottom left corner of the map)

    public int visited = -1;
    public int x = 0;
    public int y = 0;
    public int weight;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
